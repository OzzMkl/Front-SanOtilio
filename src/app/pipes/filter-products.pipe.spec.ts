import { FilterProductPipe } from './filter-products.pipe';

describe('FilterProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductPipe();
    expect(pipe).toBeTruthy();
  });
});
