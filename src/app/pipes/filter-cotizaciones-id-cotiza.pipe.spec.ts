import { FilterCotizacionesIdCotizaPipe } from './filter-cotizaciones-id-cotiza.pipe';

describe('FilterCotizacionesIdCotizaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCotizacionesIdCotizaPipe();
    expect(pipe).toBeTruthy();
  });
});
