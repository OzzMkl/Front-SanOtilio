import { FilterVendedorPipe } from './filter-vendedor.pipe';

describe('FilterVendedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVendedorPipe();
    expect(pipe).toBeTruthy();
  });
});
