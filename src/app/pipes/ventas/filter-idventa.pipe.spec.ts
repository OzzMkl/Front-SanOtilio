import { FilterIdventaPipe } from './filter-idventa.pipe';

describe('FilterIdventaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIdventaPipe();
    expect(pipe).toBeTruthy();
  });
});
