import { FilterOrdersIdordPipe } from './filter-orders-idord.pipe';

describe('FilterOrdersIdordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrdersIdordPipe();
    expect(pipe).toBeTruthy();
  });
});
