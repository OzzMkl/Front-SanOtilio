import { FilterProductsModalcePipe } from './filter-products-modalce.pipe';

describe('FilterProductsModalcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductsModalcePipe();
    expect(pipe).toBeTruthy();
  });
});
