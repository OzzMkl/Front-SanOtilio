import { FilterProductModalPipe } from './filter-productsModal.pipe'

describe('FilterProductModalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductModalPipe();
    expect(pipe).toBeTruthy();
  });
});